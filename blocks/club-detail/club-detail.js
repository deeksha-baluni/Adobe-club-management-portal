/**
 * @deprecated Use club-hero + club-activities + club-events + club-recaps + club-team + cta-banner instead.
 */
export default async function decorate(block) {
  block.innerHTML = '';
  block.hidden = true;
}
