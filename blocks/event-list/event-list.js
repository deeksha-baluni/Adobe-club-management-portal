/**
 * Event List — consolidated event body (RSVP rail + about + details + organizer).
 * da.live: key | value config rows; event data from JSON.
 */
import mountEventListBlock, { EVENT_LIST_DEFAULTS } from '../event-shared/mount-event-list.js';

export { EVENT_LIST_DEFAULTS };
export default mountEventListBlock;
